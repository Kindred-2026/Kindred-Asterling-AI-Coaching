# fly.toml app configuration for the registered Kindred staging app.
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'kindred-asterling-ai-coaching'
primary_region = 'yyz'

[env]
  NODE_ENV = 'production'
  PORT = '8080'
  DATABASE_PROVIDER = 'mongo'
  APP_PUBLIC_URL = 'https://kindred-asterling-ai-coaching.fly.dev'
  AI_PROVIDER = 'disabled'

[build]
  dockerfile = 'Dockerfile'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'off'
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

  [[http_service.checks]]
    interval = '30s'
    timeout = '5s'
    grace_period = '30s'
    method = 'GET'
    path = '/api/healthz/db'

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 1024
